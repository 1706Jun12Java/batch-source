package Question15;

public interface Mathable {
	// contains the calculator functions
	double add(Number x, Number y);

	double multiply(Number x, Number y);

	double subtract(Number x, Number y);

	double divide(Number x, Number y);

}
