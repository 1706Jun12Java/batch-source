package Question11a;

public class FloatShare {
	//create two float variables in one package to be used in another
	public float f = (float) (Math.random()*1000);
	public float g = (float) (Math.random()*1000);
}
